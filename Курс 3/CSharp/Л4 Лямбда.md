**Лямбда-функция** - функция без имени или анонимная функция.

Описывается как:
`(параметры) => {тело функции}`
или
`параметр => {тело функции}`
или
`параметр => команда`

Лябмда-функции - основа `System.Linq` (Language in Query) - местного языка запросов.